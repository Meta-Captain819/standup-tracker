# The Express backend this app's BFF forwards to. Locally, backend/.env has PORT=4000.
BACKEND_API_URL=http://localhost:4000

# Encrypts the httpOnly session cookie (JWE, dir/A256GCM). Must be at least 32 characters.
# Generate one with: openssl rand -base64 32
SESSION_SECRET=
