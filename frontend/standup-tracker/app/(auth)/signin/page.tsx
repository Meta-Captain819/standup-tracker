import { EmptyState } from "@/app/_components/EmptyState";

export default function SignInPage() {
  return (
    <EmptyState
      title="Sign-in form coming in a later phase"
      description="This route is the redirect target for unauthenticated visitors and exists to verify routing and role gating."
    />
  );
}
