import { EmptyState } from "@/app/_components/EmptyState";

export default function HistoryPage() {
  return (
    <EmptyState
      title="History is coming in a later phase"
      description="This route exists so the app shell's navigation and layout can be verified end to end."
    />
  );
}
